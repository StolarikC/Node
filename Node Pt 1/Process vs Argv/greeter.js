const args = process.argv.slice(2);
for (let arg of args) {
    console.log(`Hello, ${arg}`)
}

// node boilerplate.js Project

// Project
//     index.html
//     app.css
//     app.js
    
    // All file system operations have synchronous and asynchronous forms.
// Synchronous version means "do this thing and wait until it finishes to move on"
// "Stops any other code from running until it completes."